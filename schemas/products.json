{
    "$jsonSchema": {
        "bsonType": "object",
        "title": "Shop Products",
        "required": ["brand", "price", "name", "reference", "category", "sizeChart", "supplier", "sectionId"],
        "properties": {
            "brand": {
                "bsonType": "string",
                "description": "'brand' must be a string and is required"
            },
            "price": {
                "bsonType": "number",
                "description": "'price' must be a double and is required",
                "minimum": 0
            },
            "name": {
                "bsonType": "string",
                "description": "'name' must be a string and is required"
            },
            "reference": {
                "bsonType": "string",
                "description": "'reference' must be a string and is required"
            },
            "category": {
                "bsonType": "string",
                "description": "'category' must be a string and is required",
                "enum": ["child", "junior", "senior"]
            },
            "sizeChart": {
                "bsonType": "array",
                "description":"'sizeChart' must be an array and is required",
                "items": {
                    "bsonType": "object",
                    "required": ["size", "quantity"],
                    "properties": {
                        "size": {
                            "bsonType": "string",
                            "description": "'size' must be a string and is required"
                        },
                        "quantity": {
                            "bsonType": "int",
                            "description": "'quantity' must be an integer and is required",
                            "minimum": 0
                        }
                    }
                }
            },
            "supplier": {
                "bsonType": "object",
                "required": ["name", "city"],
                "description": "'supplier' must be an object and is required",
                "properties": {
                    "name": {
                        "bsonType": "string",
                        "description": "'name' must be a string and is required"
                    },
                    "city": {
                        "bsonType": "string",
                        "description": "'city' must be a string and is required"
                    }
                }
            },
            "sectionId": {
                "bsonType": "string",
                "description":"'sectionId' must be a string and is required"
            }
        }
    }
}
